using UnityEngine;
using TMPro;

namespace CurlingUI.v3 {
    public class CurlingRuleItem : MonoBehaviour
    {
        
        public ScriptableObject ruleData;
        public TextMeshProUGUI ruleTitleText;
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
            
        }

        // Update is called once per frame
        void Update()
        {
            
        }
    }
}
